export enum MooTypes {
  INT,
  OBJ,
  STR,
  ERR,
  LIST,
  CLEAR,
  NONE,
  FLOAT = 9,
  MAP = 10,
  ANON = 12,
  WAIF = 13,
  BOOL,
}

export enum TaskType {
  QUEUED = "queued",
  SUSPENDED = "suspended",
}
